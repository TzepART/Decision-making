{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('task.new') }}">Добавить задачу</a><br>
    <a href="{{ path('task.list') }}">Список задач</a><br>
    {% if(app.user) %}
        <div>
            {{ app.user.email }}
        </div>
    {% endif %}
{% endblock %}
