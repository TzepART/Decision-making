{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {{ form_start(form) }}
        {# render the task's only field: description #}
        <div class="form-group">
            {{ form_row(form.name) }}
        </div>

        <h3>Variants</h3>
        <div class="form-group">
            <ul class="variants list-unstyled" data-prototype="{{ form_widget(form.variants.vars.prototype)|e('html_attr') }}">
                {% for variant in form.variants %}
                    <li>{{ form_row(variant.name) }}</li>
                {% endfor %}
            </ul>
        </div>


        <h3>Criteria</h3>
        <div class="form-group">
            <ul class="criterias list-unstyled" data-prototype="{{ form_widget(form.criteria.vars.prototype)|e('html_attr') }}">
                {% for criter in form.criteria %}
                    <li>
                        {{ form_row(criter.name) }}

                        {{ form_row(criter.significance) }}</li>
                {% endfor %}
            </ul>
        </div>

        {{ form_end(form) }}
    </div>



{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        addlinkForAdd('variants');
        addlinkForAdd('criterias');

        function addlinkForAdd(list_name) {
            var $collectionHolder;
            var $addLink = $('<a href="#" class="add_'+list_name+'_link btn btn-primary">Add</a>');
            var $newLinkLi = $('<li></li>').append($addLink);

            jQuery(document).ready(function() {
                // Get the ul that holds the collection of variants
                $collectionHolder = $('ul.'.concat(list_name));

                // add the "add a variant" anchor and li to the variants ul
                $collectionHolder.append($newLinkLi);

                // count the current form inputs we have (e.g. 2), use that as the new
                // index when inserting a new item (e.g. 2)
                $collectionHolder.data('index', $collectionHolder.find(':input').length);

                $addLink.on('click', function(e) {
                    // prevent the link from creating a "#" on the URL
                    e.preventDefault();
                    addForm($collectionHolder, $newLinkLi);
                });
            });
        }

        function addForm($collectionHolder, $newLinkLi) {
            // Get the data-prototype explained earlier
            var prototype = $collectionHolder.data('prototype');

            // get the new index
            var index = $collectionHolder.data('index');

            // Replace '__name__' in the prototype's HTML to
            // instead be a number based on how many items we have
            var newForm = prototype.replace(/__name__/g, index);

            // increase the index with one for the next item
            $collectionHolder.data('index', index + 1);

            // Display the form in the page in an li, before the "Add a variant" link li
            var $newFormLi = $('<li></li>').append(newForm);
            $newLinkLi.before($newFormLi);
        }
    </script>
{% endblock %}