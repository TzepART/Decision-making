{% extends 'base.html.twig' %}

{% block body %}
    {% if tasks %}
        <ul>
            {% for task in tasks %}
                <li>{{ task.name }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}